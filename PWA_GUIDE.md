# 📱 PWA Installation Guide - คู่มือติดตั้ง App

## ✨ ระบบนับสต็อกเป็น Progressive Web App (PWA) แล้ว!

คุณสามารถติดตั้งเป็น App บนมือถือหรือคอมพิวเตอร์ได้ เพื่อ:
- ✅ เปิดแบบ fullscreen (ไม่มี browser bar)
- ✅ มี icon บน home screen  
- ✅ ทำงาน offline ได้
- ✅ เร็วกว่าเว็บปกติ
- ✅ ใช้งานง่ายเหมือน native app

---

## 📱 วิธีติดตั้งบน **Android** (Chrome/Samsung Internet)

### วิธีที่ 1: ผ่าน Prompt อัตโนมัติ
1. เปิดเว็บใน Chrome/Samsung Internet
2. รอ 10 วินาที จะมี popup ด้านล่างขึ้นมา
3. กดปุ่ม **"ติดตั้ง"**
4. เสร็จแล้ว! icon จะปรากฏบน home screen

### วิธีที่ 2: ติดตั้งเอง
1. เปิดเว็บใน Chrome
2. กดปุ่ม **⋮** (3 จุดมุมขวาบน)
3. เลือก **"Add to Home screen"** หรือ **"ติดตั้ง"**
4. ตั้งชื่อ App (เช่น "นับสต็อก")
5. กด **"Add"** หรือ **"เพิ่ม"**
6. เสร็จแล้ว!

---

## 🍎 วิธีติดตั้งบน **iPhone/iPad** (Safari)

1. เปิดเว็บใน Safari
2. กดปุ่ม **Share** (สี่เหลี่ยมมีลูกศรขึ้น) ที่ด้านล่าง
3. เลื่อนลงหา **"Add to Home Screen"**
4. ตั้งชื่อ App (เช่น "นับสต็อก")
5. กด **"Add"**
6. เสร็จแล้ว! icon จะอยู่บน home screen

**หมายเหตุ:** iPhone ต้องใช้ Safari เท่านั้น (Chrome/Firefox บน iOS ติดตั้งไม่ได้)

---

## 💻 วิธีติดตั้งบน **Desktop** (Windows/Mac)

### Chrome/Edge:
1. เปิดเว็บ
2. กดปุ่ม **⋮** หรือ **•••** (มุมขวาบน)
3. เลือก **"Install..."** หรือ **"ติดตั้ง..."**
4. กด **"Install"**
5. เสร็จแล้ว! จะมี shortcut บนเดสก์ท็อป

### Safari (Mac):
1. เปิดเว็บ
2. กด **File** → **Add to Dock**
3. เสร็จแล้ว!

---

## 🔧 คุณสมบัติของ PWA

### Offline Mode
- เก็บข้อมูลไว้ใน cache
- สแกนบาร์โค้ดได้แม้ไม่มีเน็ต
- Sync อัตโนมัติเมื่อมีเน็ตกลับมา

### Background Sync
- ข้อมูลที่ยังไม่ sync จะถูกส่งอัตโนมัติ
- ไม่ต้องกังวลเรื่องข้อมูลหาย

### Auto Update
- อัพเดท App อัตโนมัติเมื่อมีเวอร์ชันใหม่
- จะมี prompt ให้รีโหลดเพื่ออัพเดท

### Safe Area Support
- รองรับ notch บน iPhone X, 11, 12, 13, 14, 15
- UI ปรับตัวอัตโนมัติ

---

## 🎯 การใช้งานหลังติดตั้ง

### เปิด App:
- **มือถือ:** กดที่ icon บน home screen
- **Desktop:** กดที่ shortcut หรือหาใน Start Menu/Applications

### ลบ App:
- **Android:** ลากและกด "Uninstall" หรือ "ลบ"
- **iPhone:** กดค้างแล้วเลือก "Remove" → "Delete"
- **Desktop:** ลบ shortcut หรือ uninstall จาก Settings

---

## 🆕 ความแตกต่างจากเว็บปกติ

| Feature | เว็บปกติ | PWA (App) |
|---------|---------|-----------|
| Browser Bar | มี | **ไม่มี** (fullscreen) |
| Home Screen Icon | ไม่มี | **มี** |
| Offline | ไม่ได้ | **ได้** |
| ความเร็ว | ปกติ | **เร็วกว่า** |
| Auto Update | ไม่มี | **มี** |
| Background Sync | ไม่มี | **มี** |

---

## ⚙️ Advanced Settings

### ล้าง Cache:
```javascript
// เปิด Browser Console (F12) แล้วพิมพ์:
navigator.serviceWorker.getRegistration().then(reg => {
    reg.unregister();
});
caches.keys().then(keys => keys.forEach(key => caches.delete(key)));
location.reload();
```

### ดู Service Worker Status:
```
Chrome: chrome://serviceworker-internals/
Edge: edge://serviceworker-internals/
```

---

## 🐛 Troubleshooting

### App ไม่แสดง Install Prompt
- ต้องเข้าผ่าน HTTPS (ไม่ใช่ HTTP)
- Localhost สามารถใช้ได้ (สำหรับทดสอบ)
- ลอง hard refresh: **Ctrl+Shift+R** (Windows) หรือ **Cmd+Shift+R** (Mac)

### Offline Mode ไม่ทำงาน
- ตรวจสอบว่า Service Worker register สำเร็จ (ดู Console)
- ลอง unregister และ re-register (ดู Advanced Settings)

### ข้อมูลไม่ Sync
- ตรวจสอบการเชื่อมต่อ internet
- ดูที่ sync badge ว่ามีรอ sync กี่รายการ
- กดปุ่ม "Sync ทันที" เพื่อ force sync

### App เป็นเวอร์ชันเก่า
- กด refresh หลายๆ ครั้ง
- ลอง uninstall และติดตั้งใหม่
- ล้าง cache (ดู Advanced Settings)

---

## 📊 PWA Features Checklist

- ✅ Web App Manifest
- ✅ Service Worker
- ✅ HTTPS (localhost OK for dev)
- ✅ Responsive Design
- ✅ Offline Support
- ✅ Add to Home Screen
- ✅ App Icons (all sizes)
- ✅ Theme Color
- ✅ Auto Update
- ✅ Background Sync (planned)
- ✅ Push Notifications (planned)

---

## 🎉 สรุป

PWA ทำให้ระบบนับสต็อกของคุณ:
- 📱 ใช้งานง่ายเหมือน app จริงๆ
- ⚡ เร็วกว่าเว็บปกติ
- 📶 ทำงานได้แม้ไม่มีเน็ต
- 🔄 อัพเดทอัตโนมัติ
- 💾 เก็บข้อมูลปลอดภัย

**ติดตั้งวันนี้ เพื่อประสบการณ์ที่ดีที่สุด! 🚀**
