# คู่มือการ Deploy ขึ้น Vercel (แบบละเอียด)

เนื่องจากโค้ดของคุณอยู่บน GitHub แล้ว (`https://github.com/astronien/Stock-counting`) การนำขึ้น Vercel จะง่ายมากครับ ทำตามขั้นตอนทีละข้อได้เลย

## 1. เข้าสู่ระบบ Vercel
1. ไปที่ [vercel.com](https://vercel.com)
2. กด **Log In**
3. เลือก **Continue with GitHub** (สำคัญ: ต้องใช้อันนี้เพื่อให้มันเชื่อมกับ GitHub ของคุณได้)

## 2. เริ่มสร้างโปรเจกต์ใหม่
1. ในหน้า Dashboard (หน้าแรกหลังล็อกอิน), มองหาปุ่ม **"Add New..."** (มักจะอยู่ขวาบน)
2. เลือก **"Project"**

## 3. เลือก Repository (ที่เก็บโค้ด)
1. คุณจะเห็นรายการ Repository จาก GitHub ของคุณ
2. มองหาชื่อ **`Stock-counting`**
3. กดปุ่ม **"Import"** สีดำข้างหลังชื่อนั้น

**⚠️ ถ้าหาไม่เจอ:**
*   ให้กดลิงก์เล็กๆ ด้านล่างที่เขียนว่า **"Adjust GitHub App Permissions"**
*   มันจะพาไปหน้า GitHub -> ให้เลือก **"All repositories"** หรือเลือกเฉพาะ **`Stock-counting`** แล้วกด Save
*   กลับมาหน้า Vercel จะเห็นชื่อโผล่มาแล้ว

## 4. ตั้งค่าการ Deploy (Configure Project)
ในหน้านี้คุณจะเห็นช่องให้กรอกหลายอย่าง **ให้ตั้งค่าตามนี้ครับ:**

*   **Project Name**: `stock-counting` (หรือชื่ออะไรก็ได้ที่คุณชอบ)
*   **Framework Preset**: `Other` (เพราะเราเขียน HTML/JS เอง ไม่ได้ใช้ React/Next.js)
*   **Root Directory**: `.` (ไม่ต้องแก้)
*   **Build and Output Settings**:
    *   **Build Command**: (ปล่อยว่าง)
    *   **Output Directory**: (ปล่อยว่าง)
    *   **Install Command**: (ปล่อยว่าง)
*   **Environment Variables**: (ไม่ต้องใส่ เพราะเราใส่ Key ในไฟล์ `supabase-config.js` ไปแล้ว)

## 5. สั่ง Deploy
1. กดปุ่ม **"Deploy"** ใหญ่ๆ ด้านล่าง
2. รอสักครู่ (ประมาณ 1-2 นาที)
3. หน้าจอจะเปลี่ยนเป็นพลุฉลอง (Congratulations!) 🎊

## 6. เสร็จสิ้น!
*   กดที่รูปหน้าจอตัวอย่าง หรือปุ่ม **"Continue to Dashboard"**
*   คุณจะเห็น **Domain** (เช่น `stock-counting.vercel.app`)
*   คลิกที่ลิงก์นั้นเพื่อใช้งานแอปได้เลย!

---

## 💡 การอัปเดตในอนาคต
ต่อไปนี้ ถ้าคุณแก้โค้ดในเครื่องและสั่ง:
1. `git add .`
2. `git commit -m "แก้..."`
3. `git push`

**Vercel จะดึงโค้ดใหม่ไป Deploy ให้เองอัตโนมัติทันทีครับ!** ไม่ต้องทำอะไรเพิ่มเลย
