# 🗄️ Supabase Setup Guide - คู่มือติดตั้ง Database

## ✅ สถานะปัจจุบัน

**ทำสำเร็จแล้ว:**
- ✅ Supabase SDK ทำงานได้ปกติบนทุกหน้า
- ✅ การเชื่อมต่อกับ Supabase สำเร็จ
- ✅ Deploy บน Vercel สำเร็จ

**ยังต้องทำ:**
- ⏳ สร้าง Tables ใน Supabase Database

---

## 📋 ขั้นตอนการสร้าง Tables

### 1. เข้าสู่ Supabase Console

1. ไปที่ [https://supabase.com/dashboard](https://supabase.com/dashboard)
2. เข้าสู่ระบบด้วย Account ของคุณ
3. เลือก Project: **hkgsikylshyopoekgqbe**

### 2. เปิด SQL Editor

1. คลิกที่เมนู **SQL Editor** ทางด้านซ้าย
2. คลิกปุ่ม **New query**

### 3. รัน SQL Script

1. คัดลอกเนื้อหาทั้งหมดจากไฟล์ [`supabase-schema.sql`](file:///Users/astronien/Desktop/stock/supabase-schema.sql)
2. วางใน SQL Editor
3. คลิกปุ่ม **Run** (หรือกด Ctrl+Enter / Cmd+Enter)
4. รอจนกว่าจะเห็นข้อความ **Success**

### 4. ตรวจสอบ Tables

1. คลิกที่เมนู **Table Editor** ทางด้านซ้าย
2. คุณควรเห็น 2 tables:
   - ✅ `stock` - สำหรับเก็บข้อมูลสินค้า
   - ✅ `count_records` - สำหรับเก็บประวัติการนับ

---

## 📊 โครงสร้าง Tables

### Table: `stock`

| Column | Type | Description |
|--------|------|-------------|
| `id` | BIGSERIAL | Primary Key (Auto increment) |
| `serialNumber` | TEXT | S/N ของสินค้า (Unique) |
| `productName` | TEXT | ชื่อสินค้า |
| `updatedAt` | TIMESTAMP | วันที่อัพเดทล่าสุด |
| `createdAt` | TIMESTAMP | วันที่สร้างรายการ |

### Table: `count_records`

| Column | Type | Description |
|--------|------|-------------|
| `id` | BIGSERIAL | Primary Key (Auto increment) |
| `serialNumber` | TEXT | S/N ที่นับ |
| `productName` | TEXT | ชื่อสินค้า |
| `employeeId` | TEXT | รหัสพนักงาน |
| `countedAt` | TIMESTAMP | วันที่/เวลาที่นับ |
| `createdAt` | TIMESTAMP | วันที่สร้างรายการ |

---

## 🔒 Security Settings (RLS)

ระบบได้ตั้งค่า Row Level Security (RLS) เพื่อความปลอดภัย:

- ✅ อนุญาตให้ Anonymous users อ่าน/เขียน/แก้ไข/ลบข้อมูลได้
- ✅ เหมาะสำหรับใช้งานภายในองค์กร
- ⚠️ **สำคัญ:** ถ้าต้องการเพิ่มความปลอดภัย ให้แก้ไข RLS Policies ใน SQL Script

---

## 🧪 ทดสอบหลังสร้าง Tables

หลังจากรัน SQL สำเร็จแล้ว:

1. เปิดเว็บ [https://stock-counting.vercel.app/](https://stock-counting.vercel.app/)
2. ลอง **Import ข้อมูล** จาก CSV/Excel
3. ตรวจสอบว่าข้อมูลปรากฏใน Dashboard
4. ลองใช้ระบบนับสต็อก

---

## ❓ Troubleshooting

### หาก Tables ไม่ปรากฏ

1. ตรวจสอบว่ารัน SQL Script สำเร็จหรือไม่
2. ลอง Refresh หน้า Table Editor
3. ตรวจสอบ Error messages ใน SQL Editor

### หากยังเข้าถึง Tables ไม่ได้

1. ตรวจสอบว่า RLS Policies ทำงานถูกต้อง
2. ลองปิด RLS ชั่วคราว:
   ```sql
   ALTER TABLE public.stock DISABLE ROW LEVEL SECURITY;
   ALTER TABLE public.count_records DISABLE ROW LEVEL SECURITY;
   ```

---

## 📞 ติดต่อ

หากพบปัญหา สามารถ:
- ตรวจสอบ Console Logs ใน Browser (F12)
- ดู Supabase Logs ใน Dashboard
- ตรวจสอบว่า API Keys ถูกต้อง

---

**สร้างเมื่อ:** 2025-11-23  
**Project:** Stock Counting System  
**Supabase Project:** hkgsikylshyopoekgqbe
